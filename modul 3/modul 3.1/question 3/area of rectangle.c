#include<stdio.h>
int main()
{
	int l,b,area;
	printf("\nenter the length:");
	scanf("%d",&l);
	printf("\nenter the brcadth:");
	scanf("%d",&b);
	
	area= l*b;
	printf("\n %d is area of rectangle ",area);
	
}
